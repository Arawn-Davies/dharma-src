# Used by docker-compose.yml for Rails message verification/encryption.
# Generate a production value with:
#   openssl rand -hex 64
SECRET_KEY_BASE=dharma_src_local_compose_secret_key_base_replace_for_public_deployments
